import React from "react";
import { Bars3Icon } from "@heroicons/react/24/outline"; 

export const Sort = ({ className }) => {
  return (
    <Bars3Icon className={`w-[27px] h-6 text-white ${className}`} />
  );
};
